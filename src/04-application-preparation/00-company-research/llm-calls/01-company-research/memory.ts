export const memory = {
  isolation: "fresh-process",
  reads: ["application-stage vacancy identity", "public company sources"],
  writes: ["sourced company research", "Codex run trace"],
} as const;
