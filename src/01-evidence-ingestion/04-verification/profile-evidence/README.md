# Profile evidence verification

Claims are not the only facts that need provenance. This component audits every
new source-derived profile scalar, skill, and language against an exact quote in
an active source.

User-confirmed values already present before synthesis remain authoritative.
Unsupported new model values are not applied, and they create readiness
blockers. Verified provenance is persisted as `profile-evidence.json`.
