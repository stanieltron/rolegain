export const memory = {
  isolation: "fresh-process",
  reads: ["immutable source chunk", "current extraction", "blocking coverage findings"],
  writes: ["typed repair patch", "Codex run trace"],
} as const;
