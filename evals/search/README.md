# Search eval versions

Search implementations are evaluated as distinct pipelines because v1 uses
adaptive discovery plus verification while v2 uses capture-first batching.

- [`v1`](./v1/README.md): production adaptive-search regression surface.
- [`v2`](./v2/README.md): frozen capture-first benchmark surface.
