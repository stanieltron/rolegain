# Evidence ingestion v1 eval

Run `npm run eval:evidence:v1`. Artifacts are written under
`.test-artifacts/evidence-evals/v1/` and record `version: "v1"` on every trial.
Schemas come from
[`src/01-evidence-ingestion/v1/schemas.ts`](../../../src/01-evidence-ingestion/v1/schemas.ts).
