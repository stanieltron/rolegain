# Matching v1 eval

Run `npm run eval:match-requirements:v1`. The default suite includes the
requirement component, Tier-2, verification, repair, and full production flow.
Artifacts are stored below `.agent-runtime/match-requirements/runs/v1/`.
