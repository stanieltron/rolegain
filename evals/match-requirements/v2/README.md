# Matching v2 eval

Run `npm run eval:match-requirements:v2`. The default suite runs the calibrated
requirement component and full one-pass production flow against the same v3.1
corpus and grader used by v1. Artifacts are stored below
`.agent-runtime/match-requirements/runs/v2/` and reports record the selected
pipeline version.
